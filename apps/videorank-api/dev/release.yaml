apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: videorank-api
  namespace: videorank-dev
spec:
  interval: 5m
  chart:
    spec:
      chart: ./apps/videorank-api/chart
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: videorank-api-values
      valuesKey: values.yaml
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3

